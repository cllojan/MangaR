import cheerio  from 'cheerio';

type Result = string[] | Error

const getLastUpdate = () => {
    try{

    }catch(e){

    }
    let manga = {
        status:200,
        
    }
    return manga
}

export default getLastUpdate